using Microsoft.AspNetCore.SignalR;

namespace Proekspert.PhoneServiceTask.Host
{
    public class PhoneServiceHub : Hub
    {
        // The hub requires no client-side methods.
        // Its only method is server-side triggered.
    }
}
